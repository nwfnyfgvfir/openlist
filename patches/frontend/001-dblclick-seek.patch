diff --git a/src/pages/home/previews/aliyun_video.tsx b/src/pages/home/previews/aliyun_video.tsx
index 65ee68a..7967397 100644
--- a/src/pages/home/previews/aliyun_video.tsx
+++ b/src/pages/home/previews/aliyun_video.tsx
@@ -20,6 +20,7 @@ import artplayerPluginAss from "~/components/artplayer-plugin-ass"
 import Hls from "hls.js"
 import { currentLang } from "~/app/i18n"
 import { AutoHeightPlugin, VideoBox } from "./video_box"
+import { enableDblclickSeek } from "./dblclick-seek"
 import { ArtPlayerIconsSubtitle } from "~/components/icons"
 import { useNavigate } from "@solidjs/router"
 import { TiWarning } from "solid-icons/ti"
@@ -335,6 +336,7 @@ const Preview = () => {
         }
       })
       player = new Artplayer(option)
+      enableDblclickSeek(player)
       let auto_fullscreen: boolean
       switch (searchParams["auto_fullscreen"]) {
         case "true":
diff --git a/src/pages/home/previews/dblclick-seek.ts b/src/pages/home/previews/dblclick-seek.ts
new file mode 100644
index 0000000..080f235
--- /dev/null
+++ b/src/pages/home/previews/dblclick-seek.ts
@@ -0,0 +1,77 @@
+import Artplayer from "artplayer"
+
+/** Default seek step (seconds) for left/right double-click zones. */
+export const DBLCLICK_SEEK_STEP = 10
+
+/**
+ * YouTube/Bilibili-style double-click seek:
+ * - left half of the video: backward
+ * - right half of the video: forward
+ * Disables ArtPlayer's default double-click fullscreen.
+ */
+export function enableDblclickSeek(
+  player: Artplayer,
+  step: number = DBLCLICK_SEEK_STEP,
+) {
+  Artplayer.DBCLICK_FULLSCREEN = false
+
+  const showHint = (label: string, side: "left" | "right") => {
+    const existing = player.template.$player.querySelector(
+      ".openlist-dblclick-seek-hint",
+    ) as HTMLElement | null
+    if (existing) existing.remove()
+
+    const el = document.createElement("div")
+    el.className = "openlist-dblclick-seek-hint"
+    el.textContent = label
+    Object.assign(el.style, {
+      position: "absolute",
+      top: "50%",
+      [side]: "12%",
+      transform: "translateY(-50%)",
+      padding: "10px 16px",
+      borderRadius: "8px",
+      background: "rgba(0,0,0,0.55)",
+      color: "#fff",
+      fontSize: "18px",
+      fontWeight: "600",
+      pointerEvents: "none",
+      zIndex: "999",
+      transition: "opacity 0.3s",
+    } as CSSStyleDeclaration)
+    player.template.$player.appendChild(el)
+    window.setTimeout(() => {
+      el.style.opacity = "0"
+      window.setTimeout(() => el.remove(), 300)
+    }, 500)
+  }
+
+  // Prefer the video surface; fall back to player root for coordinate calc.
+  const onDblclick = (event: Event) => {
+    const e = event as MouseEvent
+    const video = player.template.$video
+    const rect = (video || player.template.$player).getBoundingClientRect()
+    if (!rect.width) return
+    const x = e.clientX - rect.left
+    const isLeft = x < rect.width / 2
+    if (isLeft) {
+      player.backward = step
+      showHint(`-${step}s`, "left")
+    } else {
+      player.forward = step
+      showHint(`+${step}s`, "right")
+    }
+  }
+
+  // ArtPlayer emits 'dblclick' on the video area (desktop).
+  player.on("dblclick", onDblclick as any)
+
+  // Mobile / some browsers: also listen on video element.
+  const video = player.template.$video
+  if (video) {
+    video.addEventListener("dblclick", onDblclick)
+    player.on("destroy", () => {
+      video.removeEventListener("dblclick", onDblclick)
+    })
+  }
+}
diff --git a/src/pages/home/previews/video.tsx b/src/pages/home/previews/video.tsx
index 84dd905..fd79430 100644
--- a/src/pages/home/previews/video.tsx
+++ b/src/pages/home/previews/video.tsx
@@ -27,6 +27,7 @@ import mpegts from "mpegts.js"
 import Hls from "hls.js"
 import { currentLang } from "~/app/i18n"
 import { AutoHeightPlugin, VideoBox } from "./video_box"
+import { enableDblclickSeek } from "./dblclick-seek"
 import { ArtPlayerIconsSubtitle } from "~/components/icons"
 import { useNavigate } from "@solidjs/router"
 import "./artplayer.css"
@@ -350,6 +351,7 @@ const Preview = () => {
 
   onMount(() => {
     player = new Artplayer(option)
+    enableDblclickSeek(player)
     createEffect(on(() => objStore.raw_url, switchUrl))
     let auto_fullscreen: boolean
     switch (searchParams["auto_fullscreen"]) {
diff --git a/src/pages/home/previews/video_box.tsx b/src/pages/home/previews/video_box.tsx
index 1b006d6..54596aa 100644
--- a/src/pages/home/previews/video_box.tsx
+++ b/src/pages/home/previews/video_box.tsx
@@ -20,6 +20,8 @@ import { BsArrowRight } from "solid-icons/bs"
 
 Artplayer.PLAYBACK_RATE = [0.5, 0.75, 1, 1.25, 1.5, 2, 3, 4]
 Artplayer.REMOVE_SRC_WHEN_DESTROY = true
+// OpenList custom: double-click left/right seeks instead of fullscreen
+Artplayer.DBCLICK_FULLSCREEN = false
 
 export const players: {
   icon: string
